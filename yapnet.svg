<!-- --><svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%"><script><![CDATA[
(()=>{var t={274(t,e,r){r.d(e,{r:()=>o});var a=r(619);async function o(t,e){let r;await (0,a.k)(e.scramjetBundlePath),await (0,a.k)(e.scramjetControllerApiPath),await (0,a.k)(e.scramjetUtilsBundlePath);let o=`${"https:"===location.protocol?"wss:":"ws:"}//${location.host}${e.wispPath}`;if("epoxy"===e.transport)await (0,a.k)(e.epoxyClientPath),r=new window.EpoxyTransport.EpoxyClient({wisp:o});else if("libcurl"===e.transport)await (0,a.k)(e.libcurlClientPath),r=new window.LibcurlTransport.LibcurlClient({wisp:o});else if("bare"===e.transport)throw Error("Bare transport not implemented yet");let{Controller:n,config:i}=window.$scramjetController;return i.injectPath=e.scramjetControllerInjectPath,i.wasmPath=e.scramjetWasmPath,i.scramjetPath=e.scramjetBundlePath,e.prefix&&(i.prefix=e.prefix),new n({serviceworker:t,transport:r})}},619(t,e,r){async function a(t){let e=await navigator.serviceWorker.register(t,{type:"classic",updateViaCache:"none"});if(await navigator.serviceWorker.ready,e.active)return e.active;if(e.installing)return await new Promise(t=>{let r=e.installing;"activated"===r.state?t():r.addEventListener("statechange",function e(){"activated"===r.state&&(r.removeEventListener("statechange",e),t())})}),e.active;if(e.waiting)return await new Promise(t=>{navigator.serviceWorker.addEventListener("controllerchange",()=>{t()},{once:!0})}),navigator.serviceWorker.controller;throw Error("No service worker found in registration")}function o(t){return new Promise((e,r)=>{let a=document.createElementNS("http://www.w3.org/1999/xhtml","script");a.src=t,a.onload=()=>e(),a.onerror=()=>r(Error("Failed to load script "+t)),(document.head||window.__yapnetScriptHost||document.documentElement).appendChild(a)})}r.d(e,{G:()=>a,k:()=>o})},364(t,e,r){r.d(e,{D7:()=>n,Ef:()=>o,G$:()=>s,ZH:()=>i,_h:()=>c,sb:()=>p,tV:()=>l,tZ:()=>a});let a="@mercuryworkshop/scramjet",o="@mercuryworkshop/scramjet-controller",n="0",i="@mercuryworkshop/epoxy-transport",s="3",l="@mercuryworkshop/libcurl-transport",c="2",p={transport:"libcurl",swPath:"/sw.js",wispPath:"/wisp/",epoxyClientPath:"/clients/epoxy-client.js",libcurlClientPath:"/clients/libcurl-client.js",bareClientPath:"/clients/bare-client.js",bootstrapInitPath:"/bootstrap-init.js",scramjetControllerApiPath:"/controller/controller.api.js",scramjetControllerInjectPath:"/controller/controller.inject.js",scramjetControllerSwPath:"/controller/controller.sw.js",scramjetBundlePath:"/scram/scramjet.js",scramjetWasmPath:"/scram/scramjet.wasm",scramjetUtilsBundlePath:"/scram/scramjet-utils.js"}}},e={};function r(a){var o=e[a];if(void 0!==o)return o.exports;var n=e[a]={exports:{}};return t[a](n,n.exports,r),n.exports}r.d=(t,e)=>{for(var a in e)r.o(e,a)&&!r.o(t,a)&&Object.defineProperty(t,a,{enumerable:!0,get:e[a]})},r.o=(t,e)=>Object.prototype.hasOwnProperty.call(t,e),r.rv=()=>"1.6.7",r.ruid="bundler=rspack@1.6.7",(()=>{var t=r(274),e=r(619),a=r(364);let o="https://cdn.jsdelivr.net/npm/",n="files";async function i(){return new Promise((t,e)=>{let r=indexedDB.open("scramjet-bootstrap",1);r.onerror=()=>e(r.error),r.onsuccess=()=>t(r.result),r.onupgradeneeded=t=>{let e=t.target.result;if(!e.objectStoreNames.contains(n)){let t=e.createObjectStore(n,{keyPath:"path"});t.createIndex("version","version",{unique:!1}),t.createIndex("timestamp","timestamp",{unique:!1})}}})}async function s(t){if(!t)throw Error("Path is required for getFile");let e=await i();return new Promise((r,a)=>{let o=e.transaction(n,"readonly"),i=o.objectStore(n).get(t);i.onerror=()=>a(i.error),i.onsuccess=()=>r(i.result||null),o.onerror=()=>a(o.error)})}async function l(t){let e=await i();return new Promise((r,a)=>{let o=e.transaction(n,"readwrite"),i=o.objectStore(n).put(t);i.onerror=()=>a(i.error),i.onsuccess=()=>r(),o.onerror=()=>a(o.error)})}async function c(t,e){let r=await fetch(`https://data.jsdelivr.com/v1/packages/npm/${t}?a`);if(!r.ok)throw Error(`Failed to fetch package info: ${r.statusText}`);let a=(await r.json()).versions.filter(t=>t.version===e||t.version.startsWith(`${e}.`));if(0===a.length)throw Error(`No versions found for ${t} with major version ${e}`);return a[0].version}async function p(t,e,r){let a=`${o}${t}@${e}${r}`,n=await fetch(a);if(!n.ok)throw Error(`Failed to download ${a}: ${n.statusText}`);return await n.arrayBuffer()}async function d(t,e,r,a,o){let n=await s(o);if(n&&n.version===e)return void console.log(`Using cached ${o} (${e})`);console.log(`Downloading ${o} from ${t}@${e}...`);let i=await p(t,e,r);await l({path:o,content:i,contentType:a,version:e,timestamp:Date.now()}),console.log(`Cached ${o} (${e})`)}if("ServiceWorkerGlobalScope"in globalThis){self.$yapnetReqBody=async function(ev){try{var q=ev&&ev.request;if(q&&!("body" in q)&&q.method!=="GET"&&q.method!=="HEAD"){var b=await q.clone().arrayBuffer();Object.defineProperty(q,"body",{configurable:!0,value:b});}}catch(e){}return ev};let t,e=!1;async function w(r){t={...a.sb,...r.config};try{let r=await c(a.Ef,t.scramjetControllerVersionPin||a.D7);console.log(r);let n=`${o}${a.Ef}@${r}/package.json`,i=await fetch(n),l=await i.json();console.log(l);let p=l.dependencies[a.tZ].replace(/^[\^~]/,"");if(await d(a.tZ,p,"/dist/scramjet.js","application/javascript",t.scramjetBundlePath),await d(a.tZ,p,"/dist/scramjet.wasm","application/wasm",t.scramjetWasmPath),await d(a.Ef,r,"/dist/controller.api.js","application/javascript",t.scramjetControllerApiPath),await d(a.Ef,r,"/dist/controller.inject.js","application/javascript",t.scramjetControllerInjectPath),await d(a.Ef,r,"/dist/controller.sw.js","application/javascript",t.scramjetControllerSwPath),"libcurl"===t.transport){let e=await c(a.tV,t.libcurlTransportVersionPin||a._h);await d(a.tV,e,"/dist/index.js","application/javascript",t.libcurlClientPath)}else if("epoxy"===t.transport){let e=await c(a.ZH,t.epoxyTransportVersionPin||a.G$);await d(a.ZH,e,"/dist/index.js","application/javascript",t.epoxyClientPath)}console.log("Bootstrap initialization complete");try{let r=await s(t.scramjetControllerSwPath);if(r){let t=new TextDecoder().decode(r.content);(0,eval)(t),e=!0,console.log("Scramjet controller loaded")}else console.error("Scramjet controller not found in cache")}catch(t){console.error("Failed to load scramjet controller:",t)}self.clients.claim().catch(()=>{}).then(()=>self.clients.matchAll({includeUncontrolled:!0})).then(t=>{t.forEach(t=>{t.postMessage({type:"init-bootstrap-done",message:{ready:!0}})})})}catch(t){console.error("Bootstrap initialization failed:",t);self.clients.matchAll({includeUncontrolled:!0}).then(a=>a.forEach(a=>a.postMessage({type:"init-bootstrap-error",message:String(t&&t.message||t)})));throw t}}addEventListener("message",t=>{"object"==typeof t.data&&null!==t.data&&("init-bootstrap"===t.data.type&&w(t.data.message),"SKIP_WAITING"===t.data.type&&self.skipWaiting())}),addEventListener("fetch",r=>{let a=new URL(r.request.url).pathname;t&&a&&r.respondWith((async()=>{if(e&&self.$scramjetController){let t=self.$scramjetController;if(t.shouldRoute(r))return t.route(await $yapnetReqBody(r))}let t=await s(a);return t?new Response(t.content,{headers:{"Content-Type":t.contentType,"Cache-Control":"public, max-age=31536000"}}):fetch(r.request)})())})}else{let r=document.currentScript;async function u(o){let n=o.filePath;if(!n&&(r&&r.src&&(n=r.src),!n))throw Error("Could not determine bootstrap file path and none was provided!");let i=await (0,e.G)(n),s={...a.sb,...o};return i.postMessage({type:"init-bootstrap",message:{config:s}}),console.log(await new Promise(t=>{let e=r=>{"object"==typeof r.data&&null!==r.data&&"init-bootstrap-done"===r.data.type&&(navigator.serviceWorker.removeEventListener("message",e),t(r.data.message))};navigator.serviceWorker.addEventListener("message",e)})),await (0,t.r)(i,s)}window.initBootstrap=u}})()})();
/* ===================== YapNet single-file proxy =====================
   Wisp endpoint : WISP_URL   (Wisp v1 websocket, must end with /)
   Target site   : TARGET_URL
   Works as both: SVG document (top of file) and classic service worker.
   ------------------------------------------------------------------ */
const __YAPNET_DEBUG__ = false;
if (!("ServiceWorkerGlobalScope" in globalThis)) {
	(async function () {
		const WISP_URL = "wss://kristenblackburnvolleyballcamps.com/socket/";
		const TARGET_URL = "https://89.116.170.52.sslip.io/";
		const SVGNS = "http://www.w3.org/2000/svg";
		const XH = "http://www.w3.org/1999/xhtml";

		const svg = document.documentElement;
		const fo = document.createElementNS(SVGNS, "foreignObject");
		fo.setAttribute("x", "0");
		fo.setAttribute("y", "0");
		fo.setAttribute("width", "100%");
		fo.setAttribute("height", "100%");
		const root = document.createElementNS(XH, "div");
		root.setAttribute(
			"style",
			"width:100%;height:100%;margin:0;position:relative;display:flex;flex-direction:column;" +
				"background:#171717;color:#dbdee1;overflow:hidden;" +
				"font-family:'Segoe UI',system-ui,-apple-system,sans-serif;font-size:13px"
		);
		const status = document.createElementNS(XH, "span");
		status.setAttribute("aria-live", "polite");
		status.setAttribute(
			"style",
			"position:absolute;width:1px;height:1px;margin:-1px;padding:0;" +
				"overflow:hidden;clip-path:inset(50%);white-space:nowrap;border:0"
		);
		status.textContent = "Starting\u2026";
		const err = document.createElementNS(XH, "div");
		err.setAttribute(
			"style",
			"display:none;position:absolute;left:0;right:0;top:0;z-index:2147483647;" +
				"padding:12px 16px;background:#3b1d1f;color:#f2b8ba;" +
				"white-space:pre-wrap;overflow:auto;max-height:40%"
		);
		const view = document.createElementNS(XH, "iframe");
		view.setAttribute("style", "flex:1 1 auto;min-height:0;border:0;width:100%;background:#fff");
		view.setAttribute("title", "YapNet");
		root.appendChild(status);
		root.appendChild(err);
		root.appendChild(view);
		fo.appendChild(root);
		svg.appendChild(fo);
		window.__yapnetScriptHost = root;

		function setStatus(t) {
			status.textContent = t;
		}
		function fail(e) {
			console.error("[yapnet-svg]", e);
			setStatus("Error");
			let msg = String((e && e.message) || e);
			if (/MIME|javascript|SecurityError|register|installation/i.test(msg)) {
				msg +=
					"\n\nThe service worker must be served with a JavaScript Content-Type." +
					" Configure the host to reply with Content-Type: application/javascript" +
					" when the request carries the header \"Service-Worker: script\"" +
					" (or Sec-Fetch-Dest: serviceworker), OR place the companion sw.js" +
					" file next to this SVG.";
			}
			err.style.display = "block";
			err.textContent = msg;
		}

		function withTimeout(p, ms, what) {
			return Promise.race([
				p,
				new Promise((_, rej) =>
					setTimeout(
						() =>
							rej(
								new Error(
									what +
										" timed out after " +
										Math.round(ms / 1000) +
										"s. Check that this host can reach cdn.jsdelivr.net."
								)
							),
						ms
					)
				),
			]);
		}

		function boot(p) {
			return new Promise((resolve, reject) => {
				const finish = (fn, v) => {
					navigator.serviceWorker.removeEventListener("message", onMsg);
					fn(v);
				};
				const onMsg = (ev) => {
					if (ev.data && ev.data.type === "init-bootstrap-error") {
						finish(reject, new Error("Service worker init failed: " + ev.data.message));
					}
				};
				navigator.serviceWorker.addEventListener("message", onMsg);
				p.then((v) => finish(resolve, v), (e) => finish(reject, e));
			});
		}

		try {
			if (!("serviceWorker" in navigator)) {
				throw new Error("Service workers are unavailable. Open this over https:// (or localhost).");
			}
			const base = location.pathname.replace(/[^/]*$/, "");
			const paths = {
				transport: "libcurl",
				wispPath: "/~unused-local-wisp-endpoint/",
				filePath: location.pathname + "?sw",
				swPath: location.pathname + "?sw",
				prefix: base + "~/sj/",
				scramjetBundlePath: base + "scram/scramjet.js",
				scramjetWasmPath: base + "scram/scramjet.wasm",
				scramjetUtilsBundlePath: base + "controller/controller.api.js",
				epoxyClientPath: base + "clients/epoxy-client.js",
				libcurlClientPath: base + "clients/libcurl-client.js",
				bareClientPath: base + "clients/bare-client.js",
				bootstrapApiPath: base + "bootstrap/api.js",
				bootstrapInitPath: base + "bootstrap/init.js",
				scramjetControllerApiPath: base + "controller/controller.api.js",
				scramjetControllerInjectPath: base + "controller/controller.inject.js",
				scramjetControllerSwPath: base + "controller/controller.sw.js",
				scramjetControllerVersionPin: "0.0.11",
				libcurlTransportVersionPin: "2.0.5",
				epoxyTransportVersionPin: "3.0.1",
			};

			setStatus("Installing worker\u2026");
			let controller;
			try {
				controller = await withTimeout(boot(initBootstrap(paths)), 120000, "Bootstrap install");
			} catch (e) {
				const msg = String((e && e.message) || e);
				if (!/MIME|javascript|SecurityError|register|Failed to load|installation/i.test(msg)) throw e;
				console.warn("[yapnet-svg] self-registration failed, trying companion sw.js:", msg);
				setStatus("Retrying with sw.js\u2026");
				controller = await withTimeout(
					boot(initBootstrap(Object.assign({}, paths, { filePath: base + "sw.js" }))),
					120000,
					"Bootstrap install (sw.js)"
				);
			}

			const lc = window.LibcurlTransport;
			if (!lc || !lc.LibcurlClient) throw new Error("libcurl transport bundle failed to load");
						if (typeof __YAPNET_DEBUG__ !== "undefined" && __YAPNET_DEBUG__) {
				navigator.serviceWorker.addEventListener("message", function (m) {
					if (m.data && m.data.$ydbg) console.log("[ydbg-sw]", JSON.stringify(m.data.$ydbg));
				});
				const co = lc.LibcurlClient.create_options;
				lc.LibcurlClient.create_options = async function (params) {
					const r = await co.call(this, params);
					console.log("[ydbg-opts]", "ret:", r ? r.length : "null", "head:", r && r.length ? Array.from(r.slice(0, 40)) : null, "in:", params && params.body ? params.body.constructor.name + ":" + (params.body.constructor === Uint8Array ? params.body.length : "") : String(params && params.body));
					return r;
				};
				const of = lc.LibcurlClient.prototype.fetch;
				lc.LibcurlClient.prototype.fetch = async function (u, init) {
					console.log("[ydbg-fetch]", init && init.method, String(u).slice(-70), "body:", init && init.body ? init.body.constructor.name : String(init && init.body));
					return of.call(this, u, init);
				};
			}

			controller.setTransport(new lc.LibcurlClient({ wisp: WISP_URL }));
setStatus("Preparing tunnel\u2026");
			await controller.wait();

			setStatus("Loading " + TARGET_URL.replace(/^https?:\/\//, "") + "\u2026");
			const frame = controller.createFrame(view);
			let loadedOnce = false;
			view.addEventListener("load", function () {
				loadedOnce = true;
				setStatus("Connected");
			});
			frame.go(TARGET_URL);
			setTimeout(function () {
				if (!loadedOnce) setStatus("Tunneling\u2026");
			}, 8000);
		} catch (e) {
			fail(e);
		}
	})();
}

/* Host this file at the ROOT of a site. Single-file hosting requires a
   response-header rule so the same URL answers service-worker fetches as
   JavaScript while normal opens stay image/svg+xml. Tested Caddy example:

   @sw { header Service-Worker script }
   @swdest { header Sec-Fetch-Dest serviceworker }
   @any { not path /never-exists }
   handle /yapnet.svg {
       header @sw >Content-Type "application/javascript; charset=utf-8"
       header @swdest >Content-Type "application/javascript; charset=utf-8"
       header @any {
           +Vary "Service-Worker, Sec-Fetch-Dest"
           defer
       }
       file_server
   }

   If your host cannot do that, drop the companion sw.js next to this file
   instead - the loader falls back to it automatically (sw.js must be the
   *patched* build output, same one that was inlined here).
*/
//]]></script></svg>
